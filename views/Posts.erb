<html>
	<body>
		<% if !@posts.blank? %>
			<ul>
				<% for post in @posts %>
					<li>
						<a href="/posts/<%= post.id %>">
							<%= post.title %>
						</a>
						<%= post.body %> 
					</li>
				<% end %>
			</ul>
		<% else %>
			<p>No posts available</p>
		<% end %>
	</body>
</html>
